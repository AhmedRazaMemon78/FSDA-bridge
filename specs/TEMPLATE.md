# Spec NNN — <title>

> Copy this file to `specs/NNN-<slug>.md` and fill in the three sections. One file per spec.
> Read `CONSTITUTION.md` first — it holds the rules common to all specs (toolchain, marshalling,
> agreement gate). Don't repeat them here; only note spec-specific deviations.

## Contract

*What this spec must deliver, and how we know it's done.*

- **Deliverable:** <the public thing — function, script, surface — produced by this spec>
- **Done when:** <objective, checkable criterion — usually the agreement gate at a stated tolerance>
- **Out of scope:** <what this spec deliberately does not cover>

## Design

*How the contract is realised.*

- **Files:** <paths created/modified, one line each>
- **Signatures / calls:** <key function signatures, the FSDA call(s), input/output shapes>
- **Marshalling notes:** <anything that crosses the MATLAB ↔ Python boundary and could break>
- **Reference oracle:** <how gold outputs are produced and where they're saved>

## Tasks

*Ordered checklist. Priorities: `#p1` blocking, `#p2` this round, `#p3` nice-to-have.*

- [ ] #p1 <task>
- [ ] #p2 <task>
- [ ] #p3 <task>

### Done

(move checked items here with a date)
